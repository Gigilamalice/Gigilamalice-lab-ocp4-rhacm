apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: usecase1-placement
  namespace: usecase1-app
  labels:
    app: usecase1
spec:
  clusterLabels:
    matchLabels:
      env: dev